import { ArrivalTimePipe } from './arrival-time.pipe';

describe('ArrivalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrivalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
